# Python
__pycache__/
*.py[cod]
*$py.class
.Python
*.so
.venv/
venv/
env/
.env

# Jupyter
.ipynb_checkpoints/
*-checkpoint.ipynb

# Heavy intermediate artefacts (regenerated by notebooks 02-03)
data/processed/
data/vectors/
outputs/blender_cache/
outputs/renders/frames/
*.blend1

# NOTE: data/raw/, outputs/renders/*.mp4, outputs/gh_meshes/, outputs/plots/,
# outputs/clusters/, and outputs/fragments_params/ are intentionally TRACKED.
# The assignment's GitHub clause requires "all source code, data and assets",
# so the four scraped datasets (~750 KB), the rendered MP4 (~10 MB), the 20
# Grasshopper-baked OBJs (~200 KB), and all generated plots/json are committed.

# Trained model artefacts
*.pt
*.pth
*.bin
*.safetensors
*.model

# OS
.DS_Store
Thumbs.db
desktop.ini

# IDE
.idea/
.vscode/
*.swp

# Rhino auto-saves
grasshopper/*.gh.bak
grasshopper/AutoSave/

# OneDrive submission staging — duplicates of data/ + outputs/ files,
# kept locally for upload but not tracked (avoid double-counting in the repo).
submission_*/datasets/
submission_*/design_tool_files/
submission_*/animation/

# API keys
.openai_key
.openai_base_url
.pixabay_key
